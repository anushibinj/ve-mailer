info:
  name: test
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_ROOT_URL}}/api/v1/test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
